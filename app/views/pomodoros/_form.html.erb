<%= form_for(pomodoro) do |f| %>
  <% if pomodoro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pomodoro.errors.count, "error") %> prohibited this pomodoro from being saved:</h2>

      <ul>
      <% pomodoro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :duration %>
    <%= f.text_field :duration %>
  </div>

  <div class="field">
    <%= f.collection_select :task_id, Task.order(:name), :id, :name, include_blank: true %>
  </div>

  <div class="actions">
    <%= f.submit 'Create', class: 'waves-effect waves-light btn' %>
  </div>
<% end %>
